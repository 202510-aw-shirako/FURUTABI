Frontend build utilities live here so the repository root is not detected as a Node app by deployment platforms.

Usage:

```bash
cd tools/frontend-build
npm install
npm run build:css
npm run build:docs
```
